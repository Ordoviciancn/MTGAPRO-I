# 美术素材说明

当前牌桌使用 `public/art/storybook-arena.png`，牌背使用 `public/mtg-card-back.png`。牌手徽章由 `AvatarCrest.tsx` 和 CSS 绘制金属边框、抽象纹章与生命值，不包含人物肖像。

卡牌图片通过 Scryfall 按需请求，不随仓库批量分发。战场、手牌和堆叠卡图保持原比例，横置仅旋转。表现层动画使用已经过可见性裁剪的 Forge 语义事件。

已停用的人物肖像与旧手动网页版已移出项目，不纳入安装包。